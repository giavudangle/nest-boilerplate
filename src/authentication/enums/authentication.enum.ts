export enum STRATEGY_TYPE {
  LOCAL_STRATEGY = 'local-strategy',
  JWT_ACCESS_TOKEN_STRATEGY = 'jwt-access-token-strategy',
  JWT_REFRESH_TOKEN_STRATEGY = 'jwt-refresh-token-strategy'
}

export enum GUARD_TYPE  {
  LOCAL_GUARD = 'local-strategy',
  JWT_ACCESS_TOKEN_GUARD = 'jwt-access-token-strategy',
  JWT_REFRESH_TOKEN_GUARD = 'jwt-refresh-token-strategy'
}

export enum AUTHENTICATION_ROUTE {
  
}

